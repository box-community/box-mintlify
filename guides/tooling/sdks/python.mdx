---
title: "Install Python SDK"
rank: 4
related_endpoints: []
related_guides:
  - authentication/jwt
  - authentication/oauth2
related_pages:
  - sdks-and-tools
required_guides: []
related_resources: []
alias_paths: []
---

import {RelatedLinks, MultiRelatedLinks} from "/snippets/related-links.jsx";

You can use Box Python SDK to call Box APIs in a Python project.

<Card href="https://github.com/box/box-python-sdk" arrow title="Learn more about Python SDK on GitHub"/>

## Installation

To install the Python SDK run the following command from your terminal
window or command prompt using [pip][pip].

```shell
pip install boxsdk
```

## JWT application

When working with a Box App that uses server-side [JWT authentication][jwt], install the following additional module:

```shell
pip install "boxsdk[jwt]"
```

[pip]: https://pypi.org/project/pip/
[jwt]: /guides/authentication/jwt
[versioning]: /guides/tooling/sdks/sdk-versioning

<RelatedLinks
  title="RELATED GUIDES"
  items={[
    { label: "JWT Auth", href: "/guides/authentication/jwt/index", badge: "GUIDE" },
    { label: "OAuth 2.0 Auth", href: "/guides/authentication/oauth2/index", badge: "GUIDE" }
  ]}
/>
